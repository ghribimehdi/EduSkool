<form method="post" action="{{ path('app_soumission_devoir_delete', {'id': soumission_devoir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soumission_devoir.id) }}">
    <button class="btn">Delete</button>
</form>
