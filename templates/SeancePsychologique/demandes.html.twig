{% extends 'baseAdmin.html.twig' %}

{% block title %}Demandes en attente{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            background-color: #FFF0F5;
        }
        .card {
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn-pink {
            background-color: #FF69B4;
            border-color: #FF69B4;
            color: white;
        }
        .btn-pink:hover {
            background-color: #FF1493;
            border-color: #FF1493;
            color: white;
        }
        .icon-pink {
            color: #FF69B4;
        }
        .table thead {
            background-color: #FF69B4;
            color: white;
        }
        .detail-label {
            color: #FF69B4;
            font-weight: bold;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <div class="card">
                    <div class="card-body">
                        <h1 class="card-title text-center mb-4 text-pink">
                            <i class="fas fa-list icon-pink"></i> Demandes de Séances en Attente
                        </h1>

                        {% if demandesEnAttente is empty %}
                            <p class="text-center text-muted">💖 Aucune demande en attente 💖</p>
                        {% else %}
                            <table class="table table-hover">
                                <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Date</th>
                                    <th>Statut</th>
                                    <th>Actions</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for demande in demandesEnAttente %}
                                    <tr>
                                        <td>{{ demande.id }}</td>
                                        <td>{{ demande.dateSeance ? demande.dateSeance|date('d/m/Y H:i') : 'Non spécifié' }}</td>
                                        <td>{{ demande.status }}</td>
                                        <td>
                                            <form method="post" action="{{ path('app_psychologue_accepter', {'id': demande.id}) }}" style="display:inline;">
                                                <input type="hidden" name="_token" value="{{ csrf_token('accepter' ~ demande.id) }}">
                                                <button type="submit" class="btn btn-pink btn-sm">
                                                    <i class="fas fa-check"></i> Accepter
                                                </button>
                                            </form>

                                            <form method="post" action="{{ path('app_psychologue_refuser', {'id': demande.id}) }}" style="display:inline;">
                                                <input type="hidden" name="_token" value="{{ csrf_token('refuser' ~ demande.id) }}">
                                                <button type="submit" class="btn btn-outline-danger btn-sm">
                                                    <i class="fas fa-times"></i> Refuser
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        {% endif %}

                        <div class="d-flex justify-content-center mt-4">
                            <a href="{{ path('app_seance_psychologique_index') }}" class="btn btn-outline-secondary me-2">
                                <i class="fas fa-arrow-left"></i> Retour
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://kit.fontawesome.com/your-fontawesome-kit.js" crossorigin="anonymous"></script>
{% endblock %}
